Pill-shaped status tag for property listings — used on cards, story overlays, and carousel covers.

```jsx
<Badge variant="novo">Novo lançamento</Badge>
<Badge variant="destaque">Oportunidade</Badge>
```

Variants: `novo` (brand green), `vendido` (near-black), `destaque` (gold).
