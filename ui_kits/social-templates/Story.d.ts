/**
 * @startingPoint section="Social Templates" subtitle="Instagram Story — property listing" viewport="360x640"
 */
export interface StoryProps {
  /** Property headline, e.g. "Apto 3 quartos" */
  title?: string;
  /** Neighborhood + city */
  location?: string;
  /** Formatted price */
  price?: string;
  /** Badge text at the top */
  badge?: string;
}
